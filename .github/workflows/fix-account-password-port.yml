name: Fix account password port

on:
  push:
    branches:
      - agent/port-matgate-account-password
    paths:
      - patches/fix-account-password-port.py

permissions:
  contents: write

jobs:
  fix-port:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Correct generated source
        run: python3 patches/fix-account-password-port.py

      - name: Commit isolated correction
        run: |
          rm -f patches/fix-account-password-port.py
          rm -f .github/workflows/fix-account-password-port.yml
          rmdir patches 2>/dev/null || true
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix account password port source escaping"
          git push origin HEAD

      - name: Validate solution
        run: dotnet test Matgate.slnx --configuration Release
