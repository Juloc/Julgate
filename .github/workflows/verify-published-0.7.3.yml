name: Verify published Julgate 0.7.3

on:
  pull_request:

permissions:
  contents: read
  packages: read

jobs:
  verify:
    name: Pull and start ghcr.io/juloc/julgate:0.7.3
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Wait for immutable image
        shell: bash
        run: |
          set -euo pipefail
          for attempt in {1..60}; do
            if docker pull ghcr.io/juloc/julgate:0.7.3; then
              exit 0
            fi
            sleep 10
          done
          echo 'Image tag 0.7.3 was not published in time.'
          exit 1

      - name: Verify image metadata
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect ghcr.io/juloc/julgate:0.7.3 --format '{{ index .Config.Labels "org.opencontainers.image.version" }}')" = "0.7.3"
          test "$(docker image inspect ghcr.io/juloc/julgate:0.7.3 --format '{{ .Config.User }}')" != ""

      - name: Start and smoke test published image
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/julgate-data
          chmod 0777 /tmp/julgate-data
          docker run --detach --name julgate-verify \
            --publish 127.0.0.1:18088:8080 \
            --volume /tmp/julgate-data:/data \
            --env ASPNETCORE_URLS=http://+:8080 \
            --env JULGATE_DATA_DIR=/data \
            --env JULGATE_ADMIN_USER=admin \
            --env JULGATE_ADMIN_PASSWORD=verification-password-123 \
            --env JULGATE_CREDENTIAL_KEY=MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY= \
            --env JULGATE_GUACAMOLE_JSON_SECRET_KEY=fedcba98765432100123456789abcdef \
            --env Guacamole__JsonSecretKey=fedcba98765432100123456789abcdef \
            --env JULGATE_REQUIRE_SECURE_COOKIES=false \
            ghcr.io/juloc/julgate:0.7.3

          for attempt in {1..60}; do
            if curl --fail --silent http://127.0.0.1:18088/healthz >/tmp/health.json; then
              break
            fi
            sleep 2
          done
          curl --fail --silent http://127.0.0.1:18088/login >/tmp/login.html
          grep --quiet 'julgate-shell-enhancements.js' /tmp/login.html
          docker exec julgate-verify test -f /app/Matgate.dll

      - name: Cleanup
        if: always()
        run: |
          docker logs julgate-verify || true
          docker rm --force julgate-verify || true
          rm -rf /tmp/julgate-data || true
